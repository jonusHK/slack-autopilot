# slack-autopilot — 프로젝트 지침

슬랙 메시지의 이모지 신호로 구동되는 **범용 개발 작업 자동화 엔진**. 대상 프로젝트의 슬랙
채널에서 사람이 ▶️ 를 단 지시를 검출해, 클라우드 루틴이 구현·PR·병합까지 끌고 간다.
**이 레포는 엔진만 안다** — 프로젝트별 판단 기준은 각 대상 레포의 `AUTOMATION.md`(정책
파일)에 산다. 첫 소비자는 sai.

## 문서 우선 (docs-first)

구현 전에 결정이 문서에 있어야 한다. 코딩 중 "왜/어떻게"가 궁금하면 코드가 아니라 문서를 본다.

- **결정**: `docs/decisions.md` (D-001~). 불변 로그 — 뒤집을 땐 새 D.
- **설계**: `docs/design/engine.md`(상태 기계·파이프라인) · `docs/design/policy-contract.md`
  (대상 레포 `AUTOMATION.md` 계약).
- **돌리는 법**: `docs/runbook/setup.md`(봇 앱·토큰·루틴 등록) — 절차와 환경만.
- 문서 헤더는 `상태: 초안` + 갱신일. 버전 태그(`v0.x`) 금지 — 이력은 Git.
- 새 결정을 내리면 로그에 먼저 반영하고 코드를 짠다.

## 절대 규칙 (엔진 불변식)

- **봇은 ▶️ 와 🚀 를 절대 붙이지 않는다** — 사람 전용 트리거. 이것이 무한 루프를 구조적으로
  막는 유일한 장치다(D-002).
- **정책 파일 없는 레포는 처리하지 않는다** — 전 불릿 ❓(D-001).
- **가드 우회(`SKIP_*`)·테스트 약화로 CI 를 통과시키지 않는다** — 그건 그린이 아니다(D-004).
- **엔진에 특정 프로젝트의 지식을 넣지 않는다** — 두 번째 프로젝트를 붙일 때 엔진 수정이
  필요했다면 분리가 샌 것이다(D-001의 회귀 테스트).

## 코딩 규칙

- 스크립트는 **python3 표준 라이브러리만** + `gh`(GitHub). pip 설치·YAML 파서 같은 외부
  의존을 두지 않는다 — 클라우드 루틴 VM 에서 준비 단계 없이 그대로 돌아야 한다(설정을
  `config.json` 으로 둔 이유도 이것).
- **상태 어휘는 `bin/emoji.py` 한 자리**에만 둔다. 이모지 문자열을 다른 파일에 하드코딩하면
  상태 정의가 조용히 갈라진다. 사람 전용 이모지 가드(`assert_bot_may_add`)를 우회하지 않는다.
- 슬랙을 부르지 않고 도는 테스트(`tests/`)를 규칙 변경과 같은 커밋에 넣는다 — 실 채널로는
  "▶️ 가 없어서 0건"밖에 확인되지 않는다.
- **시크릿·식별자는 전부 환경변수**: Slack 봇 토큰·GitHub 자격은 물론 **워크스페이스·채널·
  사용자 ID 같은 식별자도 값을 커밋하지 않는다.** 문서·코드·설정에는 **변수 이름만** 쓰고
  (`SLACK_CHANNEL_ID_<프로젝트>`), 값은 `.env`(gitignore) 또는 루틴 시크릿에만 둔다.
  터미널·로그에 값을 출력하지 않는다(이름만). 이름 목록은 `.env.example`.
- 레포는 **프라이빗**이다. 그래도 값을 커밋하지 않는 이유는 — 프라이빗은 접근 통제일 뿐
  이력에서 지워지지 않고, 나중에 공개 전환·샘플 공유 때 되돌릴 수 없기 때문이다.
- 슬랙에 쓰는 사용자 노출 문구는 대상 프로젝트의 언어·톤을 따르되, 엔진 기본 문구는 이
  레포에 상수로 모은다(산재 금지).

## 커밋

- 요청받을 때만 커밋. 커밋 메시지 말미(모델명은 실제 작업한 모델로):
  ```
  Co-Authored-By: Claude <모델명> <noreply@anthropic.com>
  ```
- docs 단위로 작은 커밋. `main` 직접 커밋·푸시(솔로). 단 **이 레포를 소비하는 자동화 루틴
  자신은 이 레포를 수정하지 않는다**(엔진이 스스로를 고치는 경로 금지).
